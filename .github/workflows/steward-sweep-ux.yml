name: Steward sweep (UX)

# Daily read-only UX walk of every live site → one findings issue per app.
# Prompt: .github/steward/sweep-ux.md. Requires CLAUDE_CODE_OAUTH_TOKEN +
# STEWARD_PAT secrets (PAT is for `gh issue create` on the app repos).

on:
  # No cron — scheduled by the focus roster (.github/steward/focus.json
  # `jobs.sweep-ux`, dispatched by steward-focus.yml's hourly tick). Adjust
  # cadence/window there — from Manager's Fleet Ops panel or the GitHub UI.
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: steward-sweep-ux
  cancel-in-progress: false

jobs:
  sweep:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check secrets
        env:
          T: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          P: ${{ secrets.STEWARD_PAT }}
        run: |
          [ -n "$T" ] || { echo "::error::CLAUDE_CODE_OAUTH_TOKEN secret is missing"; exit 1; }
          [ -n "$P" ] || { echo "::error::STEWARD_PAT secret is missing"; exit 1; }

      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Authenticate gh
        env:
          STEWARD_PAT: ${{ secrets.STEWARD_PAT }}
        run: echo "GH_TOKEN=${STEWARD_PAT}" >> "$GITHUB_ENV"

      - name: Install Claude Code + browser
        run: |
          npm install -g @anthropic-ai/claude-code
          npm install -D playwright
          npx playwright install --with-deps chromium

      - name: Run sweep
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Optional per-app admin tokens: unlock each app's client-side
          # invite/admin gate so the sweep can exercise the real UI. Any that
          # are empty just leave that gate closed (the sweep notes it).
          MANAGER_ADMIN_TOKEN: ${{ secrets.MANAGER_ADMIN_TOKEN }}
          ANALYTICS_ADMIN_TOKEN: ${{ secrets.ANALYTICS_ADMIN_TOKEN }}
          JOBTRACKER_ADMIN_TOKEN: ${{ secrets.JOBTRACKER_ADMIN_TOKEN }}
          RELAY_ADMIN_TOKEN: ${{ secrets.RELAY_ADMIN_TOKEN }}
          MODELSERVER_ADMIN_TOKEN: ${{ secrets.MODELSERVER_ADMIN_TOKEN }}
        run: |
          claude -p "$(cat .github/steward/sweep-ux.md)" \
            --dangerously-skip-permissions \
            --max-turns 120 \
            --allowedTools "Bash,Read,Glob,Grep,Write"
