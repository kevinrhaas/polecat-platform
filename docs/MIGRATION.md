# Fleet Migration Plan — adopting Polecat Shell

Order chosen by risk/effort: prove the theming extremes first, then the template twins,
then the apps with special constraints. One app per phase; each phase ends green
(smoke + screenshots + changelog entry) before the next starts.

| # | App | Effort | Notes |
|---|---|---|---|
| 1 | **games** | S (DONE — pilot) | No rail today → pure addition. Proved the `neon` palette (max theme distance from house style). Per-game pages untouched. |
| 2 | **jobtracker** | M (DONE — 2026-07-15, jobtracker#2/#3) | Full checklist shipped: ui/theme/icons/shell swapped to vendored (historical keys kept), views system stays app-local (views.js v2 extracts the rest later), waffle + What's-New in rightPanel, `jt-shell` SW cache bumped each time. Vendor copy had drifted to v0.1.0 (no sync PR since initial adoption); caught up to v0.4.0 2026-07-17 (jobtracker#11) — keep it current with future `sync-shell` dispatches. |
| 3 | **manager** | M (DONE — 2026-07-15/16) | Same template; ALSO gains the launcher-adjacent role: admin console over fleet + (later) user administration. CSS uses `--brand-a/b/c` → compat aliases cover it. Full checklist shipped: frame/theme/icons/What's-New on vendor, modal/confirmDialog/toast deleted in favor of the shell trio, CLAUDE.md gained the vendor read-only rule. Vendor copy caught up to v0.4.0 2026-07-17 (manager#14, held for Kevin — smoke's only failure was anonymous GitHub-API rate-limiting from Fleet Ops' live calls, reproduced identically on unmodified main) — keep it current with future `sync-shell` dispatches. Open follow-up: consider the shell settings framework (`defineSettings`) for the Settings view (optional, non-blocking). |
| 4 | **autoselector** | M (DONE — 2026-07-17) | 2-axis theming maps 1:1, kept the `as.theme.v1` key via `configure()`; marketing site untouched, shell adopted in `/app/`. Full checklist shipped across autoselector#26 (vendor catch-up v0.1.0→v0.4.0) and #28 (`initShell`/`appSwitcher`/`rightPanel`+`initWhatsNew`, app-local `js/shell.js` deleted, `as-shell-v1`→`v2` SW bump); CLAUDE.md gained the vendor read-only rule 2026-07-17 (autoselector#29 — the one item still open after #26/#28). Keep the vendor copy current with future `sync-shell` dispatches. |
| 5 | **polecat-app (chat)** | L (shell DONE 2026-07-15) | **ASSIGNED to a dedicated session (2026-07-15, session_01WSWYSr2Pe2SJerMcwNAJ1p) — scheduled steward runs SKIP this app** (leave its open steward/* PRs alone too). Shell adopted (polecat-app#2, shipped): frame/waffle/What's-New on the historical `polecat_theme`/`polecat`/`polecat_history` keys, legacy theme values migrated pre-paint, smoke gate added at `scripts/smoke-test.mjs`. The chat.polecat.live rename is DONE (2026-07-18, on Kevin's written go): handoff path shipped + verified (#4), Pages repointed (#14), old origin serves the data-carrying stub. Vendor copy still v0.1.0 — catch up via `sync-shell` dispatch (in flight). |
| 6 | **analytics** | L | Process migration complete (PR flow, stamping, chrome-level shell, waffle, What's-New — 2026-07-15/16). **Hourly focus runs ENABLED (focus.json) and, per Kevin 2026-07-16, the VIRIDIS TRACK IS THEIR TOP PRIORITY: work STATUS.md's ★★★★ Viridis block V6→V9 IN ORDER (Home-as-instant-analytics → demo pack → prep/load data-management jobs → scientific-honesty polish) before any other backlog — all RFP requirements must be satisfied (bid due 7/31).** One slice per run, same PR discipline, coordinate via open steward/* PRs. Strict STATUS.md conventions; the Studio three-pane workspace still waits for shell v2 (unless a session upgrades the shell itself via platform PRs); do not disturb the studio's spec/export invariants. |
| 7 | **relay** | S (DONE — 2026-07-15/17) | Technically the easiest (closest to the template; the drag-resize rail came FROM relay). Full checklist shipped 2026-07-15 (relay#19-#22): frame/theme/icons/appSwitcher/rightPanel+What's-New on vendor, `js/ui.js`/`js/icons.js` thin re-export shims over the vendored toolkit, relay's own richer dialog layer (toast Undo, record sheet, popover, confirmDialog) kept app-local pending shell v2's views/sheet work. Vendor copy caught up to v0.4.0 2026-07-17 (relay#26, `relay-shell` SW cache v1→v2) — keep it current with future `sync-shell` dispatches. |
| 8 | **polecat repo** | S (DONE — 2026-07-18) | **ASSIGNED to the same dedicated session as polecat-app — scheduled steward runs SKIP this repo.** Marketing folded into polecat-app `/welcome/` (polecat-app#3, merged on Kevin's go); the polecat repo now claims **app.polecat.live** and serves the data-carrying handoff stub (polecat#3) — keep published for months (installed PWAs pin to the old origin), NOT an archive candidate until the stub retires. |

## Per-app definition of done

- Vendored shell imported with the app's historical storage keys (no state loss).
- App-local copies of ui/theme/icons/shell deleted (only after green).
- Waffle app-switcher present; What's-New in the right panel; tiles link to detail.
- SW cache name bumped; smoke green at 390×780 + desktop; screenshots regenerated;
  changelog entry shipped; app CLAUDE.md gains the vendor read-only rule.
