Metadata labels: changelog kinds, statuses, counts. Never clickable.

```jsx
<Chip tone="feature">feature</Chip>
<Chip tone="fix">fix</Chip>
<Chip>local</Chip>
```

Tones map to the changelog vocabulary the whole fleet publishes (feature / fix / polish).
