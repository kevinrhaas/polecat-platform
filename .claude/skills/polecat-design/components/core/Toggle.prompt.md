Boolean settings — dark mode, simple mode, per-feature switches in the rail's quick panel.

```jsx
<Toggle on={dark} label="Dark mode" onChange={setDark} />
```

Pair it with a plain 13px text label to its left; the switch itself never carries visible text.
