Long, scrollable secondary content — settings, a changelog feed, a picker list.

```jsx
<Sheet title="What's new" titleIcon={<Icon name="sparkle" />} onClose={close}>…</Sheet>
<Sheet title="Filters" side="bottom" onClose={close}>…</Sheet>
```
