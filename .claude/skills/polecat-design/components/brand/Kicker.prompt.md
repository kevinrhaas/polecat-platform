Opens every section on the site: a 2–4 word framing label above the h2.

```jsx
<Kicker>Proof, not promises</Kicker>
<h2>The apps</h2>
```

12px / 800 / uppercase / +1.5px in `--brand-b`. Real examples: `The idea`, `How we build`, `What I do`, `About`, `Why this works`.
