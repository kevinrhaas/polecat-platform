The Polecat name. Never tint it with an app accent, never letter-space it wider.

```jsx
<Wordmark mark="assets/logo-mark-white.png" size={17} />
<Wordmark size={22} />           {/* root footer */}
<Wordmark variant="suite" />     {/* Polecat ↗ */}
```
