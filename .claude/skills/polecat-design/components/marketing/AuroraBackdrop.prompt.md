Mount once, at the very top of a marketing page body. This is what keeps the dark surface from reading flat.

```jsx
<AuroraBackdrop />
```

Purple appears here and nowhere else — it is ambient glow only, never text or fill.
