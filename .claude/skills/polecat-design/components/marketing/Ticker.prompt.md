A quiet credibility band under the hero — the real stack, not logos.

```jsx
<Ticker items={['Pentaho','Snowflake','Databricks','BigQuery','Kubernetes','AWS','GenAI','ETL & ELT','Open source']} />
```

It is decorative (`aria-hidden`) — never put information here that appears nowhere else.
